<%= form_for(game) do |f| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :result %>
    <%= f.text_field :result %>
  </div>

  <div class="field">
    <%= f.label :white_player_id %>
    <%= f.text_field :white_player_id %>
  </div>

  <div class="field">
    <%= f.label :black_player_id %>
    <%= f.text_field :black_player_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
